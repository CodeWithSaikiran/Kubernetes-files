apiVersion: v1
kind: Service
metadata:
  name: myservice
spec:
  type: ClusterIP
  ports:
    - port: 1111
      targetPort: 5000
  selector:
    app: python-app
------------------------------------------------------------------------------
    apiVersion: v1
kind: Service
metadata:
  name: myservice
spec:
  type: NodePort
  ports:
    - port: 1111
      targetPort: 5000
      nodePort: 30001  #nordPort range (30K - 32767)
  selector:
    app: python-app
---------------------------------------------------------------------------------
apiVersion: v1
kind: Service
metadata:
  name: myservice
spec:
  type: LoadBalancer
  ports:
    - port: 1111
      targetPort: 5000
  selector:
    app: python-app
