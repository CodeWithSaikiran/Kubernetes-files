apiVersion: v1
kind: Pod
metadata:
  name: mypod
  labels:
    app: python-app
spec:
  containers:
    - name: cont-1
      image: saikiran012/python-images:python-static-image-v1
      ports:
        - containerPort: 5000
